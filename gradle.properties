android.enableJetifier=true
android.useAndroidX=true
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2048M

# Disable KSP incremental processing to ensure multi-round symbol processing
# works correctly for Hilt @HiltViewModel in library modules (generates _Binds_LazyMapKey)
ksp.incremental=false